const BroadcastAdapter = require('./broadcast-adapter');
const CustomerAdapter = require('./customer-adapter');
const PaymentAdapter = require('./payment-adapter');

module.exports = {
  BroadcastAdapter,
  CustomerAdapter,
  PaymentAdapter
};
